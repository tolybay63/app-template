# Для очистки базы данных удаляем volume образа
docker volume rm docker-images_db_data_auth

# Собираем образ Docker
docker build -t kis-keycloak-postgres .

# Запускаем контейнер
# docker run -d -p 5434:5434 kis-keycloak-postgres